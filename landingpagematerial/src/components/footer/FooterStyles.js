import { styled, List } from '@mui/material'


export const StyledList = styled(List)({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'space-around',
})

export const BolticonStyle = {
    display: { xs: 'flex', sm: 'none' },
    color: 'yellow',
    fontSize: '4rem',
    margin: '0 auto'
}